<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Health_Shastra" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item> <!-- Text color on primary background -->

        <!-- Secondary brand color. (Normal) -->
        <item name="colorSecondary">@color/cl_normal</item> <!-- Define this in colors.xml -->
        <item name="colorSecondaryVariant">@color/cl_normal_variant</item>
        <item name="colorOnSecondary">@color/black</item> <!-- Text color on secondary background -->

        <!-- Background color -->
        <item name="android:colorBackground">@color/white</item> <!-- Background should be normal, not green -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>



    <style name="Theme.Health_Shastra_medical" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#ffffff</item>
        <item name="colorPrimaryVariant">#ffffff</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#ffffff</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ImageView.Round" parent="">
        <item name="cornerSize">50%</item>
    </style>
    <style name="ImageView.SmallRound" parent="">
        <item name="cornerSize">10%</item>
    </style>
    <style name="ImageView.roundedRectangle" parent="">
        <item name="cornerSize">5%</item>
    </style>
</resources>